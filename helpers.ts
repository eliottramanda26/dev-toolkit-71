type User = { id: number; name: string; email: string };\n\nfunction isValidEmail(email: string): boolean {\n    const regex = /^[^\s@]+@[^\s@]+\.[^\s@]+$/;\n    return regex.test(email);\n}\n\nfunction validateUserInput(user: User) {\n    if (typeof user.id !== 'number' || user.id <= 0) {\n        throw new Error('Invalid user ID');\n    }\n    if (typeof user.name !== 'string' || user.name.trim() === '') {\n        throw new Error('Invalid user name');\n    }\n    if (!isValidEmail(user.email)) {\n        throw new Error('Invalid email format');\n    }\n}\n\nfunction processUserInput(user: User) {\n    try {\n        validateUserInput(user);\n        console.log('User input is valid:', user);\n    } catch (error) {\n        console.error('Error processing user input:', error.message);\n    }\n}\n\nexport { validateUserInput, processUserInput };